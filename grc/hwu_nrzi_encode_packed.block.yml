id: hwu_nrzi_encode_packed
label: nrzi_encode_packed
category: '[hwu]'
flags: [ python ]

templates:
  imports: from gnuradio import hwu
  make: hwu.nrzi_encode_packed()
inputs:
- label: in
  domain: stream
  dtype: byte

outputs:
- label: out
  domain: stream
  dtype: byte

#  'file_format' specifies the version of the GRC yml format used in the file
#  and should usually not be changed.
file_format: 1
